apiVersion: "spec.keptn.sh/0.2.0"
kind: "Shipyard"
metadata:
  name: "{{ .Project }}"
spec:
  stages:
  {{ range $stage := .Stages }}
    - name: "{{ $stage }}"
      sequences:
        - name: "dummy"
          tasks:
            - name: "dummy"
  {{ end }}