{
  "$schema": "http://json-schema.org/draft-07/schema",
  "properties": {
    "image": {
      "properties": {
        "repository": {
          "pattern": "^[a-z][a-z0-9-./]{2,63}$"
        },
        "pullPolicy": {
          "enum": [
            "IfNotPresent",
            "Always"
          ]
        }
      }
    }
  }
}